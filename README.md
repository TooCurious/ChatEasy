# ChatEasy — Простой чат с умным агентом

Этот проект представляет собой FastAPI-приложение с единым POST-эндпоинтом `/chat`, которое интегрируется с интеллектуальным агентом на базе **Dify**, использует **OpenAI-совместимый API**, **tool calling** для получения данных о заказах и **векторный поиск по FAQ**. Интерфейс реализован через простую HTML/JS-страницу, общающуюся только с `/chat`.

---

## 🧠 Возможности

- Приём сообщений от пользователя в формате `{ "message": "..." }`
- Возврат ответов в формате `{ "reply": "..." }`
- Под капотом:
  - **LLM**: `gpt-3.5-turbo` через провайдера [OpenRouter](https://openrouter.ai)
  - **Embedding-модель**: `nomic-embed-text` через провайдера [Ollama](https://ollama.com/) для векторного поиска по базе FAQ для быстрых и точных ответов на частые вопросы
  - **Tool calling**: реализован через [FastMCP](https://github.com/ibm/fastmcp) для получения статуса заказов
  - **Агент**: сконфигурирован в [Dify](https://github.com/langgenius/dify)

## 🚀 Быстрый запуск

1. **Клонируйте репозиторий**

```bash
git clone https://github.com/your-username/ChatEasy.git
```
2. **Клонируйте репозиторий Dify**
```bash
git clone https://github.com/langgenius/dify.git
```
3. **Встройте сервисы APP и MCP в dify**
   - Настройте Docker Compose так, чтобы сервисы `app` и `mcp` были в той же сети, что и Dify
   - Для этого добавте содержимое файлов mcp/docker-compose.yaml, app/docker-compose.yaml в файл dify/docker/docker-compose-template.yaml
   - Cгенерируйте новый docker-compose.yaml запустив скрипт
```bash
./generate_docker_compose
```
   - Выполните
```bash
docker-compose up -d
```
   - Убедитесь, что все сервисы запустились
```bash
docker ps -a
```
  - Откройте в браузере UI dify localhost/app, создайте агента и подключите к нему MCP
4. **Встройте сервис Ollama в dify**
  - Для этого добавте содержимое файла ollama/docker-compose.yaml в файл dify/docker/docker-compose-template.yaml
  - сгенерируйте новый docker-compose.yaml запустив скрипт
```bash
./generate_docker_compose
```
   - Выполните
```bash
docker-compose up --build -d
```
   - Убедитесь, что все сервисы запустились
```bash
docker ps -a
```
  - Загрузите модель
```bash
docker compose exec ollama ollama pull nomic-embed-text
```
  - Откройте в браузере UI dify, добавьте embeded-модель, создайте базу знаний и подключите RAG к агенту
     
